name: example
description: An example application that demonstrates how to use your_library_package.

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter
  faceki_faceanalyze_sdk:
    path: ../

flutter:
  uses-material-design: true
